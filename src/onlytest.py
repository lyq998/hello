# coding:utf-8
import numpy as np
#noise = np.random.normal(size=(10, 20))
#print(str(noise.shape[0]) + str(noise.shape[1]))

arr = np.arange(16).reshape((4, 2, 2))
print(arr)
print(np.transpose(arr))
